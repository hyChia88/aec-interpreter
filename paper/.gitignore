# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.fls
*.fdb_latexmk

# compiled PDFs (sources are the .tex; the published copy lives in site/assets/pdfs/paper_v3.pdf)
*.pdf
# arXiv source bundles (regenerated by the release step)
*.tar.gz
# superseded v2 assembly (kept locally, not tracked)
main_v2.tex
# archived figure variants
figures/*_archive.png
